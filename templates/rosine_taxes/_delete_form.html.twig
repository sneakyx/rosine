<form method="post" action="{{ path('rosine_taxes_delete', {'taxId': rosine_tax.taxId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rosine_tax.taxId) }}">
    <button class="btn">Delete</button>
</form>
