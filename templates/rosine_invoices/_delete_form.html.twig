<form method="post" action="{{ path('rosine_invoices_delete', {'companyId': rosine_invoice.companyId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rosine_invoice.companyId) }}">
    <button class="btn">Delete</button>
</form>
