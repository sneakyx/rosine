{% extends 'base.html.twig' %}

{% block title %}RosineInvoicesPositions index{% endblock %}

{% block body %}
    <h1>RosineInvoicesPositions index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>CompanyId</th>
                <th>InvoiceId</th>
                <th>PosiId</th>
                <th>ArtNumber</th>
                <th>PosiAmmount</th>
                <th>PosiUnit</th>
                <th>PosiPrice</th>
                <th>PosiLocation</th>
                <th>PosiSerial</th>
                <th>PosiText</th>
                <th>PosiTax</th>
                <th>Done</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rosine_invoices_position in rosine_invoices_positions %}
            <tr>
                <td>{{ rosine_invoices_position.companyId ? 'Yes' : 'No' }}</td>
                <td>{{ rosine_invoices_position.invoiceId }}</td>
                <td>{{ rosine_invoices_position.posiId }}</td>
                <td>{{ rosine_invoices_position.artNumber }}</td>
                <td>{{ rosine_invoices_position.posiAmmount }}</td>
                <td>{{ rosine_invoices_position.posiUnit }}</td>
                <td>{{ rosine_invoices_position.posiPrice }}</td>
                <td>{{ rosine_invoices_position.posiLocation }}</td>
                <td>{{ rosine_invoices_position.posiSerial }}</td>
                <td>{{ rosine_invoices_position.posiText }}</td>
                <td>{{ rosine_invoices_position.posiTax ? 'Yes' : 'No' }}</td>
                <td>{{ rosine_invoices_position.done ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('rosine_invoices_positions_show', {'companyId': rosine_invoices_position.companyId}) }}">show</a>
                    <a href="{{ path('rosine_invoices_positions_edit', {'companyId': rosine_invoices_position.companyId}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="13">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('rosine_invoices_positions_new') }}">Create new</a>
{% endblock %}
