<form method="post" action="{{ path('rosine_orders_delete', {'companyId': rosine_order.companyId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rosine_order.companyId) }}">
    <button class="btn">Delete</button>
</form>
