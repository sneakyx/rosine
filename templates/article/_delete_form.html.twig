<form method="post" action="{{ path('app_article_delete', {'artNumber': article.artNumber}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.artNumber) }}">
    <button class="btn">Delete</button>
</form>
