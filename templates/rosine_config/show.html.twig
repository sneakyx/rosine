{% extends 'base.html.twig' %}

{% block title %}RosineConfig{% endblock %}

{% block body %}
    <h1>RosineConfig</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Config</th>
                <td>{{ rosine_config.config }}</td>
            </tr>
            <tr>
                <th>UserId</th>
                <td>{{ rosine_config.userId }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ rosine_config.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rosine_config_index') }}">back to list</a>

    <a href="{{ path('rosine_config_edit', {'config': rosine_config.config}) }}">edit</a>

    {{ include('rosine_config/_delete_form.html.twig') }}
{% endblock %}
